# 取到$1文件的$2属性
# 格式: key=val
function getProperty {
	echo `cat $1 | awk -F '=' '{if ($1 == "'"$2"'") print $2}'`
}

function isNum {
        if [[ -z "$1" ||  $1 == *[!0-9]* ]]; then
            echo "param [$1] Not a number!"
            exit 1
        fi
        return 0
}
